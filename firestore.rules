rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow any logged-in student or teacher to read/write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

  }
}
